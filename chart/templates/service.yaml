apiVersion: v1
kind: Service
metadata:
  name: {{ include "pydss-service.fullname" . }}
  labels:
    {{- include "pydss-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pydss-service.selectorLabels" . | nindent 4 }}
